const getters = {
  currentForecast: (state) => state.currentForecast,
  weekForecast: (state) => state.weekForecast,
  coord: (state) => state.coord,
  searchStatus: (state) => state.searchStatus
}

export default getters
